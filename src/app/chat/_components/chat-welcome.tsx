/** Empty-state placeholder shown when the chat has no messages yet */
export function ChatWelcome() {
  return (
    <div className="text-muted-foreground py-12 text-center">
      Start a conversation with AI
    </div>
  );
}
